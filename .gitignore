# Local working copies of confidential client material (deck, scorecard, extracted
# holdings) and intermediate analysis. Must never be pushed to the remote.
scratch_review/

# Node deps + QA render scratch for the template build
template/node_modules/
template/assets/preview/
template/*.pdf
template/__pycache__/
template/assets/overview.png
template/assets/new*.png

# data phase (raw pulls live on the principal's machine; the manifest is committed, files are not)
data/raw/
__pycache__/
*.pyc
.pytest_cache/
